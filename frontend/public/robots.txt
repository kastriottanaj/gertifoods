User-agent: *
Allow: /

# The account pages (cart, orders, login, register, profile, in all three
# language editions) are deliberately NOT listed here. They are kept out of
# search with a <meta name="robots" content="noindex"> instead, which only
# works if the crawler is allowed to fetch the page and read it. A Disallow
# would block that fetch, and Google can still index a disallowed URL it finds
# linked from the navbar — as a bare, content-less entry — precisely because it
# never sees the noindex.

# Backend (served by Django, not the SPA)
Disallow: /admin/
Disallow: /api/

Sitemap: https://gertifoods.com/sitemap.xml
